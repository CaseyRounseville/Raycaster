const create = (pos, vel, hitBox) => {
	return {
		pos,
		vel,
		hitBox,
    listeners: []
	};
}

const registerListener = (self, l) => {
  
};

const unregisterListener = (self, l) => {
  
};

export default {
	create
};